{
  "id": "1342fa54",
  "title": "Implement DeepAgents framework adapter",
  "tags": [
    "frameworks",
    "deepagents"
  ],
  "status": "done",
  "created_at": "2026-04-29T19:55:51.268Z"
}

## Goal

Fill out `frameworks/deepagents/` with a working DeepAgents adapter for the shared software-bugfix benchmark.

## Scope

- Replace the current `run.sh` stub with an entry point that honors `shared/contract.md`.
- Add DeepAgents-specific implementation files and dependency management (`pyproject.toml` / lockfile, or equivalent).
- Read one JSON request envelope from stdin and write exactly one JSON response envelope to stdout.
- Implement the agent loop for `shared/task-spec.md`:
  - inspect files/directories in `input.repo_path`
  - search repository contents
  - edit files in place
  - run shell test commands from the repo root
  - inspect the current diff
  - return `root_cause`, `summary`, `changed_files`, `tests_run`, `evidence`, and `confidence`
- Ensure logs/debug output go to stderr, not stdout.
- Update `frameworks/deepagents/README.md` with setup, model choice, required env vars, and quirks.

## Validation

- Run harness tests if adapter changes touch shared harness behavior.
- Run at least one cell, e.g. `just eval-new` then `just eval deepagents py-parse-duration-001`.
- Confirm the cell produces valid schema output and harness-derived scoring artifacts under `runs/CURRENT/deepagents/<case>/`.

Completed via plan: .pi/plans/done/2026-04-29-2026-04-29-deepagents-adapter.md
