# mastra

Mastra (TypeScript) — under consideration; only TS entrant in the lineup.

TODO:
- [ ] Decide whether to include
- [ ] `manifest.json` declaring entry point
- [ ] `package.json` + lockfile
- [ ] Implementation against `../../shared/task-spec.md`
- [ ] `run.sh` honoring `../../shared/contract.md`

> Note: `manifest.json` and `run.sh` are v1 stubs that exit non-zero. The real adapter is follow-on work tracked in the framework-specific TODO list.
